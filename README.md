#A React Pomodoro App

To use...
- Download the code to your local machine
- Run NPM install to install the necessary packages
- Use 'npm start' to run a local version or 'npm run build' to create a local build